import tkinter as tk 
import json 
import os
from tkinter import filedialog,messagebox,font,scrolledtext 
from sympy import sympify SympifyError 

class inputboxes:
    def __init__(pick,frames,buttons, commands):
        pick.frames = frames
        pick.buttons = buttons 
        pick.commands = commands

    def new_box(pick):
        pick.

